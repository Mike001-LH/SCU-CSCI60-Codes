#include <iostream>
using namespace std;

template <typename T>
T maxOfThree(T a, T b, T c) {
    // TODO 1: return the maximum of a, b, and c using comparison operators
}

int main() {
    cout << maxOfThree(3, 5, 2) << endl;
    cout << maxOfThree(3.1, 5.8, 5.6) << endl;
    cout << maxOfThree('a', 'z', 'm') << endl;
}
